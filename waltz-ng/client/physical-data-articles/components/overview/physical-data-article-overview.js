const bindings = {
    article: '<',
    owningApp: '<',
    organisationalUnit: '<'
};

const template = require('./physical-data-article-overview.html');

const component = {
    template,
    bindings
};

export default component;