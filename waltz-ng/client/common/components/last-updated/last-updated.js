const bindings = {
    lastUpdatedBy: '<',
    lastUpdatedAt: '<'
};


const template = require('./last-updated.html');


const component = {
    bindings,
    template,
};


export default component;