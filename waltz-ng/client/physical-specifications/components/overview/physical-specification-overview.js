const bindings = {
    specification: '<',
    owningEntity: '<',
    organisationalUnit: '<'
};

const template = require('./physical-specification-overview.html');

const component = {
    template,
    bindings
};

export default component;