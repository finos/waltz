database.url=jdbc:sqlserver://$DB_HOST:$DB_PORT;databaseName=$DB_NAME;encrypt=true;trustServerCertificate=true
database.user=$DB_USER
database.password=$DB_PASSWORD
database.schema=$DB_SCHEME
database.driver=com.microsoft.sqlserver.jdbc.SQLServerDriver

jooq.dialect=SQLSERVER2014

database.pool.max=16

# Log a stack trace if a connection is held > 60s without being returned (leak detection)
database.pool.leak.detection.threshold.ms=60000

waltz.from.email=$WALTZ_FROM_EMAIL
waltz.base.url=$WALTZ_BASE_URL

oauth.token_url=$OAUTH_TOKEN_URL
oauth.client_secret=$OAUTH_CLIENT_SECRET
oauth.code_verifier=$OAUTH_CODE_VERIFIER
oauth.userinfo_url=$OAUTH_USERINFO_URL
oauth.redirect_uri=$OAUTH_REDIRECT_URI
